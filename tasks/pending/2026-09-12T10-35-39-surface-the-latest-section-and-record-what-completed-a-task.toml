type = "sub-task"
parent = "2026-09-12T08-45-00-task-failure-state.toml"
title = "Surface the latest section and record what completed a task"
goal = "A task file shows its most recent section number at the top, and a completed task records which section it was completed under."
model = "qwen3.8-27b on pond via ask-llm bridge — two small fields and their wiring"
agent = "pond-qwen"
status = "pending"
created = "2026-09-12 10:35:39"
background = "Sections live below the task's own fields, so a task that has failed and been revised gives no sign of it at the top of the file. Two scalar fields fix that: one naming the latest section, one recording which section the task was eventually completed under. Together they let a reader see at a glance that a task took two attempts and which correction carried it."
changes = ["Add latest_section to Task in scripts/task.py, a string defaulting to empty, holding the number of the highest-numbered section", "Add completed_by_section to Task, a string defaulting to empty", "Both are scalars and must serialize above the section tables — the writer already guarantees that ordering, so no serializer change is needed", "Update scripts/record_failure.py to set latest_section to the number of the section it just added", "In scripts/complete.py, set completed_by_section to the current value of latest_section when a task is completed, leaving it empty if the task has no sections", "Do not add either field to render — they are for a person reading the file, not for the executing model", "Add tests: recording a failure sets latest_section; recording a second failure updates it; completing a task with sections sets completed_by_section to the latest section number; completing a task with no sections leaves it empty; neither field appears in render output"]
files_to_read = ["scripts/task.py", "scripts/record_failure.py", "scripts/complete.py", "tests/test_complete.py"]
open_questions = ["latest_section holds the number only, not the type. If that proves ambiguous when both failures and revisions exist, say so in results rather than changing it."]
recommended_approach = "Read only the four files listed. Do not explore the repository. Use edit_file per change; never write_file."
done_when = ["Recording a failure sets latest_section to that section's number", "A second failure updates latest_section", "Completing a task with sections sets completed_by_section", "Completing a task with no sections leaves completed_by_section empty", "Neither field appears in render output", "The full suite passes: .venv/bin/python3 -m pytest -q"]
preflight = '''
## Pre-flight ⏳ L1 (local)

- Spec: 595 tokens
- Files: task.py (990), record_failure.py (428), complete.py (283), test_complete.py (721) → 2,422 total
- Reasoning buffer: 12,000 (estimated)
- Estimated total: ~15,017 tokens
- Complexity: L1 — fits comfortably — well within context window
'''
deprecated_by = ""
hallucinating_agent = ""
hallucination_reporter = ""
hallucination_reason = ""
execution_mode = "local"
worktree_path = ""
worktree_branch = ""
