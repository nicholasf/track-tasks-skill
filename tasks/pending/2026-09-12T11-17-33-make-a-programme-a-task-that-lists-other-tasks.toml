title = "Make a programme a task that lists other tasks"
goal = "A programme is an ordinary Task carrying a list of other tasks, so it parses, shows, and round-trips like any other."
model = "qwen3.8-27b on pond via ask-llm bridge — one field and a guard"
agent = "pond-qwen"
status = "pending"
created = "2026-09-12 11:17:33"
background = "Programme files are currently a hand-authored convention with no model behind them: they carry title, goal and sub_tasks but no model or agent, so Task.model_validate rejects them. This breaks main.py show, which globs every .toml in a bucket directory and parses each one as a Task — a single programme file in tasks/pending makes the whole command raise. A programme needs no separate type: it is just a task that happens to list other tasks."
changes = ["Add sub_tasks to Task in scripts/task.py: a list of strings defaulting to empty, holding the paths of the tasks the programme coordinates", "Keep it simple — a list of path strings, not a nested model. A programme is a task with sub_tasks populated; an ordinary task leaves it empty", "Do not add a separate programme type or class", "Do not add sub_tasks to render", "In scripts/show.py, make parse_task tolerant: if a file cannot be parsed as a Task, skip it rather than raising, so one malformed file cannot break the whole listing", "Add tests: a Task with sub_tasks round-trips through to_toml and from_toml; sub_tasks defaults to empty; sub_tasks does not appear in render output; and show skips a file that cannot be parsed instead of raising"]
files_to_read = ["scripts/task.py", "scripts/show.py"]
open_questions = ["Existing programme files carry title, goal, status, sub_tasks as an array of tables with slug/path/done, and no model or agent. After this change they will still fail validation, because model and agent remain required and the sub_tasks shape differs. Whether to migrate them is not settled — do not migrate anything, and note in results that they remain unparseable."]
recommended_approach = "Two small changes: one field on Task, one guard in show.py. Read only the two files listed. Use edit_file per change; do not rewrite either file. Do not explore the repository."
done_when = ["Task.sub_tasks exists and defaults to empty", "A Task with sub_tasks round-trips unchanged", "sub_tasks does not appear in render output", "main.py show pending does not raise when an unparseable .toml is present", "The full suite passes: .venv/bin/python3 -m pytest -q"]
preflight = '''
## Pre-flight ⏳ L1 (local)

- Spec: 627 tokens
- Files: task.py (990), show.py (1,154) → 2,144 total
- Reasoning buffer: 12,000 (estimated)
- Estimated total: ~14,771 tokens
- Complexity: L1 — fits comfortably — well within context window
'''
deprecated_by = ""
hallucinating_agent = ""
hallucination_reporter = ""
hallucination_reason = ""
execution_mode = "local"
worktree_path = ""
worktree_branch = ""
